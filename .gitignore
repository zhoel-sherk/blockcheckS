# Python
__pycache__/
*.py[cod]
dist/
build/
*.egg-info/
*.so

# IDE
.vscode/
.idea/
.DS_Store
.cursor/
.cursor-audit/

# Project — runtime state, logs, campaign artifacts
state.db
state.db-journal
state.db-wal
state.db-shm
# Root-level campaign DBs (e.g. week_cov.db); logs/*.db covered by logs/
/*.db
/*.db-journal
/*.db-wal
/*.db-shm
run.lock
*.lock
logs/
output/
.env
reports/
tmp-scripts/

# Secrets / local credentials (settings.example.* stay tracked)
token.txt
settings.ini

# Packet captures (dev/diag; blobs/ stays tracked)
*.pcap
*.pcapng
*.sqlite
*.sqlite3

# pytest / lint
.pytest_cache/
.ruff_cache/
.hypothesis/
.mutmut-cache/
.mypy_cache/
html/
mutants/

# venv
.venv/

# coverage
.coverage
coverage.xml
htmlcov/

# local tooling (act GitHub runner, etc.)
bin/

# local agent onboarding file (not committed)
AGENTS.md

# Local agent notes — root /mcp/ only, NOT src/blockchecks/mcp/
/mcp/
*_audit.md
