# blockcheckS user configuration (~/.config/blockcheckS/config.toml)
# Copy to: ~/.config/blockcheckS/config.toml
# Priority: CLI args > BLOCKCHECKS_* env > this file > XDG defaults

[paths]
db = "~/.local/share/blockcheckS/state.db"
out_dir = "~/.local/share/blockcheckS/export"

[run]
parallel = 4
scan_level = "fast"
# timeout = 5.0
# max = 0

[tools]
nfqws2 = "/opt/zapret2/nfq2/nfqws2"
blobs = "/opt/zapret2/blobs"
# python = "/path/to/.venv/bin/python"
# proxy = "socks5://127.0.0.1:11080"

[secure_dns]
# enabled = true
# doh_server = "https://cloudflare-dns.com/dns-query"

# Optional: copy ./state.db to XDG path on first run if XDG db missing
# [paths.migrate]
# legacy = true
